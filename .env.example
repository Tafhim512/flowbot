# Database (SQLite by default, switch to PostgreSQL by changing the URL)
DATABASE_URL=sqlite:///./flowbot.db

# LLM provider
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-3.5-turbo

# App settings
LOG_LEVEL=INFO
APP_ENV=development
